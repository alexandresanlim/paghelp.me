<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PixQrCodeGeneratorOffline.Views.PaymentPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:PixQrCodeGeneratorOffline.Controls"
    xmlns:icons="clr-namespace:PixQrCodeGeneratorOffline.Helpers.Icon"
    xmlns:qr="clr-namespace:Xam.Forms;assembly=Xam.Forms.QRCode"
    xmlns:vm="clr-namespace:PixQrCodeGeneratorOffline.ViewModels"
    x:DataType="vm:PaymentViewModel"
    BackgroundColor="{DynamicResource background_page}"
    Shell.NavBarIsVisible="False">

    <ContentPage.Content>

        <Grid RowDefinitions="*,auto">

            <!--<Grid.Background>
                <LinearGradientBrush EndPoint="0,1">
                    <GradientStop Color="{DynamicResource primary}"
                          Offset="0.1" />
                    <GradientStop Color="{DynamicResource primaryDark}"
                          Offset="1.0" />
                </LinearGradientBrush>
            </Grid.Background>-->

            <ScrollView Grid.Row="0" Padding="15,0">
                <StackLayout VerticalOptions="Start">

                    <StackLayout Orientation="Horizontal">

                        <StackLayout Padding="0,15,30,10">
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" />
                            </StackLayout.GestureRecognizers>

                            <controls:CustomIcon
                                FontSize="{DynamicResource FontSizeLarge}"
                                Glyph="{x:Static icons:FontAwesomeSolid.ChevronLeft}"
                                IconColor="{DynamicResource textPrimary}"
                                VerticalOptions="CenterAndExpand" />
                        </StackLayout>


                        <StackLayout
                            Padding="30,15,0,10"
                            HorizontalOptions="EndAndExpand"
                            IsVisible="{Binding SaveButtonVisible}"
                            Spacing="0">
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SaveCommand}" />
                            </StackLayout.GestureRecognizers>
                            <controls:CustomIcon
                                FontSize="{DynamicResource FontSizeLarge}"
                                Glyph="{x:Static icons:FontAwesomeSolid.Save}"
                                IconColor="{DynamicResource textPrimary}" />
                            <Label
                                FontFamily="{DynamicResource FontBold}"
                                Text="Salvar"
                                TextColor="{DynamicResource textPrimary}" />
                        </StackLayout>
                    </StackLayout>

                    <Label
                        FontFamily="{DynamicResource FontBold}"
                        FontSize="{DynamicResource FontSizeMedium}"
                        Text="Mostre o QR Code ou campartilhe o código copia e cola"
                        TextColor="{DynamicResource textPrimary}" />


                    <StackLayout Spacing="15">
                        <Label
                            FontSize="{DynamicResource FontSizeSmall}"
                            Text="O pagador precisa abir o app que vai fazer a transferência PIX e escanear este QR Code ou colar o código copia e cola."
                            TextColor="{DynamicResource textSecondary}" />

                        <qr:QRCode
                            BackgroundColor="White"
                            Content="{Binding CurrentPixPaylod.QrCode}"
                            HeightRequest="300"
                            HorizontalOptions="CenterAndExpand"
                            Level="H"
                            VerticalOptions="CenterAndExpand"
                            WidthRequest="300"
                            Color="#000000">

                            <qr:QRCode.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PaymentViewModel}}, Path=SharePayloadCommand}" />
                            </qr:QRCode.GestureRecognizers>

                        </qr:QRCode>

                    </StackLayout>

                    <Label
                        FontFamily="{DynamicResource FontBold}"
                        FontSize="35"
                        HorizontalOptions="Center"
                        IsVisible="{Binding CurrentPixPaylod.PixCob.Viewer.ValuePresentation, Converter={StaticResource HasDataConverter}}"
                        Text="{Binding CurrentPixPaylod.PixCob.Viewer.ValuePresentation}"
                        TextColor="{DynamicResource textPrimary}" />

                    <Label
                        FontFamily="{DynamicResource FontBold}"
                        FontSize="{DynamicResource FontSizeMedium}"
                        HorizontalOptions="Center"
                        Text="{Binding CurrentPixPaylod.PixKey.Viewer.NamePresentation, StringFormat='Para: {0}'}"
                        TextColor="{DynamicResource textPrimary}" />

                    <Label
                        FontSize="{DynamicResource FontSizeSmall}"
                        HorizontalOptions="Center"
                        Text="{Binding CurrentPixPaylod.PixKey.Viewer.InstitutionPresentation, StringFormat='Instituição: {0}'}"
                        TextColor="{DynamicResource textPrimary}" />

                    <Label
                        FontSize="{DynamicResource FontSizeMicro}"
                        HorizontalOptions="Center"
                        Text="{Binding CurrentPixPaylod.PixKey.Viewer.KeyPresentation}"
                        TextColor="{DynamicResource textPrimary}" />
                </StackLayout>
            </ScrollView>

            <StackLayout
                Grid.Row="1"
                Padding="10"
                BackgroundColor="{Binding CurrentPixPaylod.PixKey.FinancialInstitution.Institution.MaterialColor.Primary}">
                <StackLayout.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PaymentViewModel}}, Path=SharePayloadCommand}" />
                </StackLayout.GestureRecognizers>

                <Label
                    FontFamily="{DynamicResource FontBold}"
                    FontSize="{DynamicResource FontSizeSmall}"
                    Text="Código copia e cola"
                    TextColor="{Binding CurrentPixPaylod.PixKey.FinancialInstitution.Institution.MaterialColor.TextOnPrimary}" />

                <Frame
                    Padding="15"
                    BackgroundColor="{Binding CurrentPixPaylod.PixKey.FinancialInstitution.Institution.MaterialColor.PrimaryDark}"
                    CornerRadius="10">
                    <Grid ColumnDefinitions="*, auto">
                        <Label
                            Grid.Column="0"
                            FontSize="{StaticResource FontSizeMicro}"
                            LineBreakMode="TailTruncation"
                            MaxLines="2"
                            Text="{Binding CurrentPixPaylod.QrCode}"
                            TextColor="{Binding CurrentPixPaylod.PixKey.FinancialInstitution.Institution.MaterialColor.TextOnPrimary}" />
                        <controls:CustomIcon
                            Grid.Column="1"
                            FontSize="{StaticResource FontSizeMedium}"
                            Glyph="{x:Static icons:FontAwesomeSolid.ShareSquare}"
                            IconColor="{Binding CurrentPixPaylod.PixKey.FinancialInstitution.Institution.MaterialColor.TextOnPrimary}"
                            VerticalOptions="CenterAndExpand" />
                    </Grid>
                </Frame>
            </StackLayout>

        </Grid>

    </ContentPage.Content>
</ContentPage>