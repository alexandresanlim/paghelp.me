<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:controls="clr-namespace:PixQrCodeGeneratorOffline.Controls" 
             xmlns:qr="clr-namespace:Xam.Forms;assembly=Xam.Forms.QRCode"
             xmlns:vm="clr-namespace:PixQrCodeGeneratorOffline.ViewModels"
             xmlns:icons="clr-namespace:PixQrCodeGeneratorOffline.Helpers.Icon" 
             x:Class="PixQrCodeGeneratorOffline.Views.PaymentPage"
             BackgroundColor="{Binding CurrentPixPaylod.PixKey.FinancialInstitution.Institution.MaterialColor.Primary}"
             >
    
    <ContentPage.Content>

        <Grid RowDefinitions="*,auto" Style="{StaticResource paddingPageStackLayout}">

            <!--<Grid.Background>
                <LinearGradientBrush EndPoint="0,1">
                    <GradientStop Color="{DynamicResource primary}"
                          Offset="0.1" />
                    <GradientStop Color="{DynamicResource primaryDark}"
                          Offset="1.0" />
                </LinearGradientBrush>
            </Grid.Background>-->

            <ScrollView Grid.Row="0" Padding="15,0">
                <StackLayout>
                    
                    <StackLayout Orientation="Horizontal">

                        <StackLayout Padding="0,15,30,10">
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                            </StackLayout.GestureRecognizers>

                            <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ChevronLeft }" IconSize="Title" IconColor="{DynamicResource textOnPrimary}" VerticalOptions="CenterAndExpand" />
                        </StackLayout>


                        <StackLayout Padding="30,15,0,10" HorizontalOptions="EndAndExpand" Spacing="0" IsVisible="{Binding SaveButtonVisible}">
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SaveCommand}" />
                            </StackLayout.GestureRecognizers>
                            <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Save }" IconSize="Large" IconColor="{DynamicResource textOnPrimary}" />
                            <controls:CustomLabel Text="Salvar" TextColor="{DynamicResource textOnPrimary}" FontAttributes="Bold" />
                        </StackLayout>
                    </StackLayout>
                    
                    <controls:CustomLabel Text="Mostre o QR Code ou campartilhe o código copia e cola" FontAttributes="Bold" FontSize="Large" TextColor="{DynamicResource textOnPrimary}" />


                    <StackLayout Spacing="15">
                        <controls:CustomLabel Text="O pagador precisa abir o app que vai fazer a transferência PIX e escanear este QR Code ou colar o código copia e cola." FontSize="Small" TextColor="{DynamicResource textOnPrimary}" />

                        <qr:QRCode
                            Content="{Binding CurrentPixPaylod.QrCode}"
                            BackgroundColor="White"
                            Color="#000000" 
                            Level="H" 
                            WidthRequest="300" 
                            HeightRequest="300" 
                            VerticalOptions="CenterAndExpand" 
                            HorizontalOptions="CenterAndExpand" 
                            >

                            <qr:QRCode.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PaymentViewModel}}, Path=SharePayloadCommand}" />
                            </qr:QRCode.GestureRecognizers>

                        </qr:QRCode>

                    </StackLayout>

                    <controls:CustomLabel Text="{Binding CurrentPixPaylod.PixCob.Viewer.ValuePresentation}" IsVisible="{Binding CurrentPixPaylod.PixCob.Viewer.ValuePresentation,Converter={StaticResource HasDataConverter}}" FontSize="35" TextColor="{DynamicResource textOnPrimary}" HorizontalOptions="Center" FontAttributes="Bold" />

                    <controls:CustomLabel Text="{Binding CurrentPixPaylod.PixKey.Viewer.NamePresentation, StringFormat='Para: {0}'}" TextColor="{DynamicResource textOnPrimary}" FontSize="Medium"  HorizontalOptions="Center" FontAttributes="Bold" />

                    <controls:CustomLabel Text="{Binding CurrentPixPaylod.PixKey.Viewer.InstitutionPresentation, StringFormat='Instituição: {0}'}" TextColor="{DynamicResource textOnPrimary}" FontSize="Small"  HorizontalOptions="Center" />

                    <controls:CustomLabel Text="{Binding CurrentPixPaylod.PixKey.Viewer.KeyPresentation}" TextColor="{DynamicResource textOnPrimary}" FontSize="Micro"  HorizontalOptions="Center" />
                </StackLayout>
            </ScrollView>

            <StackLayout Grid.Row="1" Padding="10" BackgroundColor="{Binding CurrentPixPaylod.PixKey.FinancialInstitution.Institution.MaterialColor.Primary}">
                <StackLayout.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PaymentViewModel}}, Path=SharePayloadCommand}" />
                </StackLayout.GestureRecognizers>

                <controls:CustomLabel Text="Código copia e cola" FontAttributes="Bold" FontSize="Small" TextColor="{DynamicResource textOnPrimary}" />

                <Frame BackgroundColor="{DynamicResource secondary}" CornerRadius="10" Padding="15">
                    <Grid ColumnDefinitions="*, auto">
                        <controls:CustomLabel Grid.Column="0" Text="{Binding CurrentPixPaylod.QrCode}" FontSize="Micro" MaxLines="2" LineBreakMode="TailTruncation" TextColor="{DynamicResource textOnSecondary}" />
                        <controls:CustomIcon Grid.Column="1" Glyph="{x:Static icons:FontAwesomeSolid.ShareSquare }" IconSize="Medium" IconColor="{DynamicResource textOnSecondary}" VerticalOptions="CenterAndExpand" />
                    </Grid>
                </Frame>
            </StackLayout>

        </Grid>

    </ContentPage.Content>
</ContentPage>