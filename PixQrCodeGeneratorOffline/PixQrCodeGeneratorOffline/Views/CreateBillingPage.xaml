<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:controls="clr-namespace:PixQrCodeGeneratorOffline.Controls" 
             xmlns:qr="clr-namespace:Xam.Forms;assembly=Xam.Forms.QRCode"
             xmlns:vm="clr-namespace:PixQrCodeGeneratorOffline.ViewModels" 
             x:Class="PixQrCodeGeneratorOffline.Views.CreateBillingPage"
             xmlns:icons="clr-namespace:PixQrCodeGeneratorOffline.Helpers.Icon" 
             xmlns:local="clr-namespace:PixQrCodeGeneratorOffline.Converters"
             BackgroundColor="{DynamicResource primary}"
             Shell.NavBarIsVisible="False"
             >

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:CurrencyConverter x:Key="CurrencyConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="10" Spacing="15">

                <StackLayout>
                    <controls:CustomLabel Text="Valor:" FontSize="Title" FontAttributes="Bold" Margin="10,0,0,0" TextColor="{DynamicResource textOnPrimary}" />
                    
                    <controls:CustomFrame BackgroundColor="{DynamicResource backgroundTransparent}" Padding="15" CornerRadius="15">
                        <StackLayout Spacing="15">
                            <Grid ColumnDefinitions="*, auto">

                                <controls:CustomFrame Grid.Column="0" BackgroundColor="{DynamicResource backgroundTransparent}" Padding="10,5" CornerRadius="10">
                                    <controls:CustomEntry Placeholder="R$0,00" Text="{Binding CurrentPixKey.Value, Converter={StaticResource CurrencyConverter}}" Unfocused="CustomEntry_Unfocused" Keyboard="Numeric" FontSize="Large" ReturnType="Next" MaxLength="11" TextColor="{DynamicResource textOnBackgroundTransparent}" PlaceholderColor="{DynamicResource textOnBackgroundTransparent}" />

                                </controls:CustomFrame>

                                <StackLayout Grid.Column="1" Spacing="2">
                                    <controls:CustomFrame BackgroundColor="{DynamicResource backgroundTransparent}" Padding="10" CornerRadius="15,15,0,0">
                                        <controls:CustomFrame.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CreateBillingViewModel}}, Path=AddValueCommand}" />
                                        </controls:CustomFrame.GestureRecognizers>
                                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.PlusCircle }" IconColor="{DynamicResource textOnBackgroundTransparent}" IconSize="Small" />
                                    </controls:CustomFrame>
                                    <controls:CustomFrame BackgroundColor="{DynamicResource backgroundTransparent}" Padding="10" CornerRadius="0,0,15,15">
                                        <controls:CustomFrame.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CreateBillingViewModel}}, Path=RmValueCommand}" />
                                        </controls:CustomFrame.GestureRecognizers>
                                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.MinusCircle }" IconColor="{DynamicResource textOnBackgroundTransparent}" IconSize="Small" />
                                    </controls:CustomFrame>
                                </StackLayout>
                            </Grid>
                            <controls:CustomLabel Text="*(Opcional) Se nenhum valor for informado, o pagador poderá informar qualquer valor." FontSize="Small" TextColor="{DynamicResource textOnBackgroundTransparent}" />
                        </StackLayout>
                    </controls:CustomFrame>
                </StackLayout>

                <StackLayout>
                    <controls:CustomLabel Text="Solicitação Pagador:" FontAttributes="Bold" FontSize="Title" Margin="10,0,0,0" TextColor="{DynamicResource textOnPrimary}" />

                    <controls:CustomFrame BackgroundColor="{DynamicResource backgroundTransparent}" Padding="15" CornerRadius="15">
                        <StackLayout Spacing="15">
                            <controls:CustomFrame BackgroundColor="{DynamicResource backgroundTransparent}" Padding="10,5" CornerRadius="10">
                                <controls:CustomEntry Text="{Binding CurrentPixKey.Description}" Unfocused="CustomEntry_Unfocused" ReturnType="Done" TextColor="{DynamicResource textOnBackgroundTransparent}" />
                            </controls:CustomFrame>
                            <controls:CustomLabel Text="*(Opcional) Texto que aparecerá para o pagador no momento do pagamento." FontSize="Small" TextColor="{DynamicResource textOnBackgroundTransparent}" />
                        </StackLayout>
                    </controls:CustomFrame>

                </StackLayout>

                <StackLayout>
                    <controls:CustomLabel Text="QRCode Gerado:" FontAttributes="Bold" FontSize="Title" Margin="10,0,0,0" TextColor="{DynamicResource textOnPrimary}" />

                    <controls:CustomFrame BackgroundColor="{DynamicResource backgroundTransparent}" Padding="15" CornerRadius="15">
                        <StackLayout Spacing="15">
                            <controls:CustomLabel Text="Mostre o QRCode para o pagador ou compartilhe o código copia e cola" FontSize="Small" TextColor="{DynamicResource textOnBackgroundTransparent}" />

                            <qr:QRCode
                            Content="{Binding CurrentPixKey.Payload}"
                            BackgroundColor="White"
                            Color="#000000" 
                            Level="H" 
                            WidthRequest="300" 
                            HeightRequest="300" 
                            VerticalOptions="CenterAndExpand" 
                            HorizontalOptions="CenterAndExpand" 
                            >

                                <qr:QRCode.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CreateBillingViewModel}}, Path=SharePayloadCommand}" />
                                </qr:QRCode.GestureRecognizers>

                            </qr:QRCode>


                            <StackLayout>
                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CreateBillingViewModel}}, Path=SharePayloadCommand}" />
                                </StackLayout.GestureRecognizers>

                                <controls:CustomLabel Text="Código copia e cola" FontAttributes="Bold" FontSize="Small" TextColor="{DynamicResource textOnBackgroundTransparent}" />

                                <controls:CustomFrame BackgroundColor="{DynamicResource backgroundTransparent}" Padding="15" CornerRadius="10">
                                    <Grid ColumnDefinitions="*, auto">
                                        <controls:CustomLabel Grid.Column="0" Text="{Binding CurrentPixKey.Payload}" FontSize="Micro" MaxLines="2" LineBreakMode="TailTruncation" TextColor="{DynamicResource textOnBackgroundTransparent}" />
                                        <controls:CustomIcon Grid.Column="1" Glyph="{x:Static icons:FontAwesomeSolid.ShareSquare }" IconSize="Medium" IconColor="{DynamicResource textOnBackgroundTransparent}" VerticalOptions="CenterAndExpand" />
                                    </Grid>
                                </controls:CustomFrame>
                            </StackLayout>

                        </StackLayout>
                    </controls:CustomFrame>
                </StackLayout>

                

            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>