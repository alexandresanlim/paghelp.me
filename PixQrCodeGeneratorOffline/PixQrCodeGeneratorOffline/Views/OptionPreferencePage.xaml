<?xml version="1.0" encoding="utf-8" ?>
<shared:ContentPageWithNavBar  xmlns:shared="clr-namespace:PixQrCodeGeneratorOffline.Views.Shared"
                               xmlns="http://xamarin.com/schemas/2014/forms"
                               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                               xmlns:controls="clr-namespace:PixQrCodeGeneratorOffline.Controls"
                               x:Class="PixQrCodeGeneratorOffline.Views.OptionPreferencePage"
                               BackgroundColor="{DynamicResource primary}">
    
    <ContentPage.Content>

        <StackLayout Padding="25,0" Spacing="15">

            <StackLayout.Background>
                <LinearGradientBrush EndPoint="0,1">
                    <GradientStop Color="{DynamicResource primary}"
                          Offset="0.1" />
                    <GradientStop Color="{DynamicResource primaryDark}"
                          Offset="1.0" />
                </LinearGradientBrush>
            </StackLayout.Background>

            <Label Text="Preferências" Style="{DynamicResource optionTitle}" />

            <StackLayout Spacing="0">
                <StackLayout Padding="0,15">
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding OptionStyleListCommand}"/>
                        </StackLayout.GestureRecognizers>
                        <Label Text="Tela inicial" Style="{DynamicResource subOptionTitle}" />
                    </StackLayout>

                <StackLayout Padding="0,15">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding OptionFingerPrintCommand}"/>
                    </StackLayout.GestureRecognizers>
                    <Label Text="Biometria" Style="{DynamicResource subOptionTitle}" />
                </StackLayout>

                <StackLayout Padding="0,15">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding OptionPDVCommand}"/>
                    </StackLayout.GestureRecognizers>
                    <Label Text="Modo PDV" Style="{DynamicResource subOptionTitle}" />
                </StackLayout>
            </StackLayout>
        </StackLayout>

    </ContentPage.Content>
</shared:ContentPageWithNavBar>