<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:controls="clr-namespace:PixQrCodeGeneratorOffline.Controls"
             x:Class="PixQrCodeGeneratorOffline.Views.AddPixKeyPage"
             xmlns:icons="clr-namespace:PixQrCodeGeneratorOffline.Helpers.Icon"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{DynamicResource primaryDark}"
             >
    <ContentPage.Content>
        <!--<StackLayout>-->
            <!--<controls:CustomFrame.Background>
                <LinearGradientBrush EndPoint="0,1">
                    <GradientStop Color="{DynamicResource primary}"
                          Offset="0.1" />
                    <GradientStop Color="{DynamicResource primaryDark}"
                          Offset="1.0" />
                </LinearGradientBrush>
            </controls:CustomFrame.Background>-->
        
        <Grid RowDefinitions="auto,*,auto" RowSpacing="0">
            <StackLayout Grid.Row="0" Spacing="0" Padding="15,0,0,0" Orientation="Horizontal">
                <controls:CustomLabel Text="Editar Chave" FontAttributes="Bold" TextColor="{DynamicResource textOnPrimary}" VerticalOptions="Center" HorizontalOptions="StartAndExpand" FontSize="Medium" />

                <StackLayout Padding="10" IsVisible="{Binding IsEdit}">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding DeleteCommand}" />
                    </StackLayout.GestureRecognizers>
                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.TrashAlt }" IconSize="Medium" IconColor="{DynamicResource textOnPrimary}" />
                    <controls:CustomLabel Text="Excluir" TextColor="{DynamicResource textOnPrimary}" FontAttributes="Bold" />
                </StackLayout>
                <StackLayout Padding="10">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SaveCommand}" />
                    </StackLayout.GestureRecognizers>
                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Save }" IconSize="Medium" IconColor="{DynamicResource textOnPrimary}" />
                    <controls:CustomLabel Text="Salvar" TextColor="{DynamicResource textOnPrimary}" FontAttributes="Bold" />
                </StackLayout>
            </StackLayout>
            
            <StackLayout Grid.Row="1" Padding="15,0,15,15">
                <controls:CustomLabel Text="IMPORTANTE" FontAttributes="Bold" TextColor="{DynamicResource textOnPrimary}" />
                    
                <controls:CustomCarouselView
                                IndicatorView="indicatorView"
                                ItemsSource="{Binding Notices}"
                                IsScrollAnimated="True"
                                Loop="False"
                                >

                    <controls:CustomCarouselView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="#10000000" Padding="10" CornerRadius="5">
                                <controls:CustomLabel Text="{Binding .}" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" TextColor="{DynamicResource textOnPrimary}"/>
                            </Frame>

                        </DataTemplate>
                    </controls:CustomCarouselView.ItemTemplate>
                </controls:CustomCarouselView>

                <IndicatorView x:Name="indicatorView"
                                       IndicatorsShape="Circle"
                                       IndicatorColor="LightGray"
                                       SelectedIndicatorColor="DarkGray" />
            </StackLayout>

                

                <ScrollView Grid.Row="2" VerticalOptions="FillAndExpand">
                <!--<StackLayout Grid.Row="2" VerticalOptions="Fill" BackgroundColor="Green">-->
                    <controls:CustomFrame  CornerRadius="35,0,0,0" BackgroundColor="{DynamicResource primary}" VerticalOptions="FillAndExpand">

                        <!--<controls:CustomFrame.Background>
                            <LinearGradientBrush EndPoint="0,1">
                                <GradientStop Color="{DynamicResource primary}"
                          Offset="0.1" />
                                <GradientStop Color="{DynamicResource primaryDark}"
                          Offset="1.0" />
                            </LinearGradientBrush>
                        </controls:CustomFrame.Background>-->

                        <StackLayout Padding="15,30">

                            <StackLayout Spacing="0">
                                <controls:CustomLabel Text="Instituição Financeira" FontSize="Medium" FontAttributes="Bold" Margin="10,0,0,0" TextColor="{DynamicResource textOnPrimary}" />
                                <Frame BackgroundColor="{DynamicResource secondary}" Padding="15" CornerRadius="10">
                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding SelectedInstitutionCommand}" />
                                    </Frame.GestureRecognizers>
                                    <StackLayout Orientation="Horizontal">
                                        <controls:CustomLabel Text="{Binding CurrenSelectedFinancialInstitutionText}" FontAttributes="Bold" FontSize="Medium" TextColor="{DynamicResource textOnSecondary}" HorizontalOptions="FillAndExpand" />
                                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ChevronDown }" IconSize="Medium" IconColor="{DynamicResource textOnSecondary}" />
                                    </StackLayout>

                                </Frame>
                            </StackLayout>

                            <StackLayout Spacing="0">
                                <controls:CustomLabel Text="Chave" FontSize="Medium" FontAttributes="Bold" Margin="10,0,0,0" TextColor="{DynamicResource textOnPrimary}" />
                                <Frame BackgroundColor="{DynamicResource secondary}" Padding="10,5" CornerRadius="10">
                                    <controls:CustomEntry Placeholder="{Binding CurrenKeyPlaceholder}" FontSize="Medium" Text="{Binding CurrentPixKey.Key}" ReturnType="Next" TextColor="{DynamicResource textOnSecondary}" PlaceholderColor="LightGray" />
                                </Frame>
                            </StackLayout>

                            <StackLayout Spacing="0">
                                <controls:CustomLabel Text="Nome" FontSize="Medium" FontAttributes="Bold" Margin="10,0,0,0" TextColor="{DynamicResource textOnPrimary}" />
                                <Frame BackgroundColor="{DynamicResource secondary}" Padding="10,5" CornerRadius="10">
                                    <controls:CustomEntry Placeholder="Digite o nome" Text="{Binding CurrentPixKey.Name}" ReturnType="Next" TextColor="{DynamicResource textOnSecondary}" PlaceholderColor="LightGray" />
                                </Frame>
                            </StackLayout>

                            <StackLayout Spacing="0">
                                <controls:CustomLabel Text="Cidade (opcional)" FontSize="Medium" FontAttributes="Bold" Margin="10,0,0,0" TextColor="{DynamicResource textOnPrimary}" />
                                <Frame BackgroundColor="{DynamicResource secondary}" Padding="10,5" CornerRadius="10">
                                    <controls:CustomEntry Placeholder="Digite a cidade" Text="{Binding CurrentPixKey.City}" ReturnType="Done" TextColor="{DynamicResource textOnSecondary}" PlaceholderColor="LightGray" />
                                </Frame>
                            </StackLayout>

                        </StackLayout>
                    </controls:CustomFrame>
                <!--</StackLayout>-->
                </ScrollView>


            </Grid>

        <!--</StackLayout>-->
        
       
    </ContentPage.Content>
</ContentPage>