<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:controls="clr-namespace:PixQrCodeGeneratorOffline.Controls"
             x:Class="PixQrCodeGeneratorOffline.Views.AddPixKeyPage"
             xmlns:icons="clr-namespace:PixQrCodeGeneratorOffline.Helpers.Icon"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{DynamicResource primaryLight}"
             >

    <ContentPage.Resources>
        <ResourceDictionary>

            <Style x:Key="styleInputFrame" TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{DynamicResource foreground_page}"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="CornerRadius" Value="10"/>
            </Style>

            <Style x:Key="styleInputTitle" TargetType="Label">
                <Setter Property="FontSize" Value="{DynamicResource FontSizeMedium}"/>
                <Setter Property="FontFamily" Value="FontPoppinsSemiBold"/>
                <Setter Property="Margin" Value="10,0,0,0"/>
                <Setter Property="TextColor" Value="{DynamicResource textPrimary}"/>
            </Style>

        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ContentPage.Content>
        <!--<StackLayout>-->
            <!--<controls:CustomFrame.Background>
                <LinearGradientBrush EndPoint="0,1">
                    <GradientStop Color="{DynamicResource primary}"
                          Offset="0.1" />
                    <GradientStop Color="{DynamicResource primaryDark}"
                          Offset="1.0" />
                </LinearGradientBrush>
            </controls:CustomFrame.Background>-->
        
        <Grid RowDefinitions="auto,0.4*,*" RowSpacing="0">
            <StackLayout Grid.Row="0" Spacing="0" Padding="15,0,0,0" Orientation="Horizontal" BackgroundColor="{DynamicResource primary}">

                <StackLayout Padding="0,0,30,0">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                    </StackLayout.GestureRecognizers>

                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ChevronLeft }" IconSize="Title" IconColor="{DynamicResource textOnPrimary}" VerticalOptions="CenterAndExpand" />
                </StackLayout>

                <controls:CustomLabel Text="Editar Chave" FontAttributes="Bold" TextColor="{DynamicResource textOnPrimary}" VerticalOptions="Center" HorizontalOptions="StartAndExpand" FontSize="Medium" />

                <StackLayout Padding="10" IsVisible="{Binding IsEdit}">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding DeleteCommand}" />
                    </StackLayout.GestureRecognizers>
                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.TrashAlt }" IconSize="Medium" IconColor="{DynamicResource textOnPrimary}" />
                    <controls:CustomLabel Text="Excluir" TextColor="{DynamicResource textOnPrimary}" FontAttributes="Bold" />
                </StackLayout>
                <StackLayout Padding="10">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SaveCommand}" />
                    </StackLayout.GestureRecognizers>
                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Save }" IconSize="Medium" IconColor="{DynamicResource textOnPrimary}" />
                    <controls:CustomLabel Text="Salvar" TextColor="{DynamicResource textOnPrimary}" FontAttributes="Bold" />
                </StackLayout>
            </StackLayout>
            
            <StackLayout Grid.Row="1" Padding="15">
                <controls:CustomLabel Text="IMPORTANTE" FontAttributes="Bold" TextColor="{DynamicResource textOnPrimary}" />
                    
                <controls:CustomCarouselView
                                IndicatorView="indicatorView"
                                ItemsSource="{Binding Notices}"
                                IsScrollAnimated="True"
                                Loop="False"
                                >

                    <controls:CustomCarouselView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="#10000000" Padding="10" CornerRadius="5">
                                <controls:CustomLabel Text="{Binding .}" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" TextColor="{DynamicResource textOnPrimary}"/>
                            </Frame>

                        </DataTemplate>
                    </controls:CustomCarouselView.ItemTemplate>
                </controls:CustomCarouselView>

                <IndicatorView x:Name="indicatorView"
                                       IndicatorsShape="Circle"
                                       IndicatorColor="{DynamicResource primary}"
                                       SelectedIndicatorColor="{DynamicResource primaryDark}" />
            </StackLayout>

                

                <!--<ScrollView Grid.Row="2" VerticalOptions="FillAndExpand">-->
                <!--<StackLayout Grid.Row="2" VerticalOptions="Fill" BackgroundColor="Green">-->
            
            <controls:CustomFrame Grid.Row="2"  CornerRadius="35,35,0,0" BackgroundColor="{DynamicResource background_page}" VerticalOptions="FillAndExpand">

                        <!--<controls:CustomFrame.Background>
                            <LinearGradientBrush EndPoint="0,1">
                                <GradientStop Color="{DynamicResource primary}"
                          Offset="0.1" />
                                <GradientStop Color="{DynamicResource primaryDark}"
                          Offset="1.0" />
                            </LinearGradientBrush>
                        </controls:CustomFrame.Background>-->

                <ScrollView>
                    <StackLayout Padding="15,30">

                        <StackLayout Spacing="0">
                            <Label Text="Instituição Financeira" Style="{DynamicResource styleInputTitle}" />
                            <Frame Style="{DynamicResource styleInputFrame}" Padding="15">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding SelectedInstitutionCommand}" />
                                </Frame.GestureRecognizers>
                                <StackLayout Orientation="Horizontal">
                                    <controls:CustomLabel Text="{Binding CurrenSelectedFinancialInstitutionText}" FontAttributes="Bold" FontSize="Medium" TextColor="{DynamicResource textPrimary}" HorizontalOptions="FillAndExpand" />
                                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ChevronDown }" IconSize="Medium" IconColor="{DynamicResource textPrimary}" />
                                </StackLayout>

                            </Frame>
                        </StackLayout>

                        <StackLayout Spacing="0">
                            <Label Text="Chave" Style="{DynamicResource styleInputTitle}" />
                            <Frame Style="{DynamicResource styleInputFrame}">
                                <controls:CustomEntry Placeholder="{Binding CurrenKeyPlaceholder}" FontSize="Medium" Text="{Binding CurrentPixKey.Key}" ReturnType="Next" />
                            </Frame>
                        </StackLayout>

                        <StackLayout Spacing="0">
                            <Label Text="Nome" Style="{DynamicResource styleInputTitle}" />
                            <Frame Style="{DynamicResource styleInputFrame}">
                                <controls:CustomEntry Placeholder="Digite o nome" Text="{Binding CurrentPixKey.Name}" ReturnType="Next"/>
                            </Frame>
                        </StackLayout>

                        <StackLayout Spacing="0">
                            <Label Text="Cidade (opcional)" Style="{DynamicResource styleInputTitle}" />
                            <Frame Style="{DynamicResource styleInputFrame}">
                                <controls:CustomEntry Placeholder="Digite a cidade" Text="{Binding CurrentPixKey.City}" ReturnType="Done" />
                            </Frame>
                        </StackLayout>

                    </StackLayout>
                </ScrollView>
            
            </controls:CustomFrame>
                <!--</StackLayout>-->
                <!--</ScrollView>-->
        </Grid>

        <!--</StackLayout>-->
        
       
    </ContentPage.Content>
</ContentPage>