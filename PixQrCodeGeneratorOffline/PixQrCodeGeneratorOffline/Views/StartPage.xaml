<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:qr="clr-namespace:Xam.Forms;assembly=Xam.Forms.QRCode" 
             xmlns:controls="clr-namespace:PixQrCodeGeneratorOffline.Controls"
             xmlns:icons="clr-namespace:PixQrCodeGeneratorOffline.Helpers.Icon" 
             xmlns:content="clr-namespace:PixQrCodeGeneratorOffline.Views.Content" 
             xmlns:collections="clr-namespace:PixQrCodeGeneratorOffline.Views.Collections"
             x:Class="PixQrCodeGeneratorOffline.Views.StartPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{DynamicResource background_page}"
             >


    <ContentPage.Resources>
        <ResourceDictionary>


            <Style x:Key="styleActionText" TargetType="Label">
                <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
                <Setter Property="TextColor" Value="{DynamicResource textSecondary}"/>
                <Setter Property="FontFamily" Value="{DynamicResource FontSemiBold}" />
                <Setter Property="VerticalOptions" Value="CenterAndExpand" />
            </Style>

            <Style x:Key="styleActionIcon" TargetType="controls:CustomIcon">
                <Setter Property="Size" Value="{DynamicResource FontSizeSmall}"/>
                <Setter Property="IconColor" Value="{DynamicResource textSecondary}" />
            </Style>

            <Style x:Key="styleTextOnButtonActionListContact" TargetType="Label">
                <Setter Property="TextColor" Value="{DynamicResource textSecondary}"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
                <Setter Property="FontSize" Value="{DynamicResource FontSizeMicro}"/>
                <Setter Property="FontFamily" Value="{DynamicResource FontSemiBold}" />

            </Style>

            <Style x:Key="styleIconActionListContact" TargetType="controls:CustomIcon">
                <Setter Property="Size" Value="{DynamicResource FontSizeMedium}"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
                <Setter Property="IconColor" Value="{DynamicResource textSecondary}"/>
            </Style>


            <Style x:Key="styleButtonActionListContact" TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{DynamicResource textSecondary}"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="10,15"/>

            </Style>

        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>



        <ScrollView>
            <StackLayout>
                <StackLayout Padding="15" BackgroundColor="{DynamicResource primary}">
                    <Label Text="Olá, Boa Tarde!" FontSize="{DynamicResource FontSizeLarge}" FontFamily="{DynamicResource FontBold}" TextColor="{DynamicResource textOnPrimary}" />
                    <Label Text="Conexão" />
                    <Label Text="Dark Mode" />
                </StackLayout>

                <BoxView HeightRequest="1" BackgroundColor="{DynamicResource foreground_page}" HorizontalOptions="FillAndExpand" />

                <StackLayout Grid.Column="0" Spacing="15" Padding="10">
                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Key }" HorizontalOptions="Start" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                    <Label Text="Minhas Chaves" FontSize="{DynamicResource FontSizeMedium}" FontFamily="{DynamicResource FontBold}" TextColor="{DynamicResource textSecondary}" />
                </StackLayout>



                <CarouselView ItemsSource="{Binding PixKeyList}"
                              PeekAreaInsets="20,0"
                              CurrentItem="{Binding CurrentPixKey}"
                              CurrentItemChangedCommand="{Binding ChangeSelectPixKeyCommand}"
                              HeightRequest="150"
                              IndicatorView="indicatorView"
                              Loop="True"
                              >

                    <!--<CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Horizontal"/>
                    </CollectionView.ItemsLayout>-->

                    <CarouselView.ItemTemplate>
                        <DataTemplate>


                            <StackLayout Padding="0,0,10,0">
                                <Frame CornerRadius="10" BackgroundColor="{DynamicResource foreground_page}" Padding="10">
                                    <StackLayout>



                                        <Grid ColumnDefinitions="auto,*">

                                            <StackLayout Grid.Column="0" WidthRequest="8" BackgroundColor="{Binding FinancialInstitution.Institution.MaterialColor.Primary}" />

                                            <StackLayout Grid.Column="1" Padding="10,15" Spacing="15">


                                                <Grid ColumnDefinitions="auto,*" ColumnSpacing="15">

                                                    <qr:QRCode
                                                    Grid.Column="0"
                                                    Content="{Binding Payload.QrCode}"
                                                    BackgroundColor="White"
                                                    Color="#000000" 
                                                    Level="H" 
                                                        WidthRequest="80"
                                                        HeightRequest="80"
                                                    VerticalOptions="Center" 
                                                    >

                                                        <qr:QRCode.GestureRecognizers>
                                                            <TapGestureRecognizer Command="{Binding Command.NavigateToPaymentPageCommand}" />
                                                        </qr:QRCode.GestureRecognizers>

                                                    </qr:QRCode>


                                                    <Grid Grid.Column="1" ColumnDefinitions="auto, *" RowDefinitions="auto, auto, auto">

                                                        <controls:CustomIcon Grid.Row="0" Grid.Column="0" Glyph="{x:Static icons:FontAwesomeSolid.University }" Style="{DynamicResource styleActionIcon}" />
                                                        <Label Grid.Row="0" Grid.Column="1" Text="{Binding Viewer.InstitutionPresentation}" Style="{DynamicResource styleActionText}" />

                                                        <controls:CustomIcon Grid.Row="1" Grid.Column="0" Glyph="{x:Static icons:FontAwesomeSolid.UserAlt }" Style="{DynamicResource styleActionIcon}" />
                                                        <Label Grid.Row="1" Grid.Column="1" Text="{Binding Viewer.NamePresentation}" Style="{DynamicResource styleActionText}" />

                                                        <controls:CustomIcon Grid.Row="2" Grid.Column="0" Glyph="{x:Static icons:FontAwesomeSolid.Key }" Style="{DynamicResource styleActionIcon}" />
                                                        <Label Grid.Row="2" Grid.Column="1" MaxLines="1" LineBreakMode="MiddleTruncation" Text="{Binding Viewer.KeyPresentation}" Style="{DynamicResource styleActionText}" />

                                                    </Grid>

                                                    <!--<StackLayout Grid.Column="2" Padding="30,5,0,15" VerticalOptions="Start">
                                                        <StackLayout.GestureRecognizers>
                                                            <TapGestureRecognizer Command="{Binding Command.EditKeyCommand}" />
                                                        </StackLayout.GestureRecognizers>

                                                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Edit }" IconColor="{DynamicResource textSecondary}"/>

                                                        <Label Text="Editar" Style="{DynamicResource styleTextOnButtonActionList}" TextColor="{DynamicResource textSecondary}"/>
                                                    </StackLayout>-->

                                                </Grid>


                                            </StackLayout>
                                        </Grid>



                                    </StackLayout>
                                </Frame>
                            </StackLayout>


                        </DataTemplate>
                    </CarouselView.ItemTemplate>
                </CarouselView>

                <IndicatorView x:Name="indicatorView"
                                       IndicatorsShape="Circle"
                                       IndicatorColor="LightGray"
                                       SelectedIndicatorColor="DarkGray" />


                <CollectionView ItemsSource="{Binding CurrentPixKeyActions}" HeightRequest="130" Margin="0,15,0,0">

                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Horizontal"/>
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout Padding="5,0">
                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Command}" />
                                </StackLayout.GestureRecognizers>

                                <Frame
                                    BackgroundColor="{DynamicResource foreground_page}"
                                    CornerRadius="40"
                                    HeightRequest="50"
                                    WidthRequest="50"
                                    IsClippedToBounds="True"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center"
                                    Padding="15"
                                    >
                                    <StackLayout VerticalOptions="Center">
                                        <controls:CustomIcon Glyph="{Binding Icon}" IconColor="{Binding Colors.Primary}" Size="25" />
                                    </StackLayout>
                                </Frame>

                                <Label Text="{Binding Title}" FontFamily="{DynamicResource FontSemiBold}" TextColor="{Binding textSecondary}" HorizontalOptions="Center" FontSize="{DynamicResource FontSizeMicro}" />
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                </CollectionView>


                <Grid ColumnDefinitions="auto,*, auto" Padding="10" ColumnSpacing="10">
                    
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding NavigateToAddNewKeyPageCommand}" />
                    </Grid.GestureRecognizers>

                    <StackLayout Grid.Column="0" Spacing="15">
                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.PlusCircle }" VerticalOptions="CenterAndExpand" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                    </StackLayout>
                    
                    <StackLayout Grid.Column="1" Spacing="15">
                        <Label Text="Adicionar Nova" FontSize="{DynamicResource FontSizeMedium}" TextColor="{DynamicResource textSecondary}" />
                    </StackLayout>

                    <StackLayout Grid.Column="2" Spacing="15" Padding="30,0,0,0">
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateToAddNewKeyPageCommand}" />
                        </StackLayout.GestureRecognizers>

                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ChevronRight }" HorizontalOptions="End" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" VerticalOptions="CenterAndExpand" />
                    </StackLayout>

                </Grid>

                <Grid ColumnDefinitions="auto,*, auto" Padding="10" ColumnSpacing="10">

                    <StackLayout Grid.Column="0" Spacing="15">
                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Share }" VerticalOptions="CenterAndExpand" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                    </StackLayout>

                    <StackLayout Grid.Column="1" Spacing="15">
                        <Label Text="Compartilhar Todas" FontSize="{DynamicResource FontSizeMedium}" TextColor="{DynamicResource textSecondary}" />
                    </StackLayout>

                    <StackLayout Grid.Column="2" Spacing="15" Padding="30,0,0,0">
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateToAddNewKeyPageCommand}" />
                        </StackLayout.GestureRecognizers>

                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ChevronRight }" HorizontalOptions="End" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                    </StackLayout>

                </Grid>

                <Grid ColumnDefinitions="auto,*, auto" Padding="10" ColumnSpacing="10">

                    <StackLayout Grid.Column="0" Spacing="15">
                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Trash }" VerticalOptions="CenterAndExpand" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                    </StackLayout>

                    <StackLayout Grid.Column="1" Spacing="15">
                        <Label Text="Excluir Todas" FontSize="{DynamicResource FontSizeMedium}" TextColor="{DynamicResource textSecondary}" />
                    </StackLayout>

                    <StackLayout Grid.Column="2" Spacing="15" Padding="30,0,0,0">
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateToAddNewKeyPageCommand}" />
                        </StackLayout.GestureRecognizers>

                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ChevronRight }" HorizontalOptions="End" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                    </StackLayout>

                </Grid>


                <BoxView HeightRequest="1" BackgroundColor="{DynamicResource foreground_page}" HorizontalOptions="FillAndExpand" />
                
                <StackLayout Padding="10" Spacing="15">
                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.HandHoldingUsd }" HorizontalOptions="Start" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                    <Label Text="Cobranças Salvas" FontSize="{DynamicResource FontSizeMedium}" FontFamily="{DynamicResource FontBold}" TextColor="{DynamicResource textSecondary}" />
                </StackLayout>

                <collections:BillingSaveHorizontalCollectionView />


                <BoxView HeightRequest="1" BackgroundColor="{DynamicResource foreground_page}" HorizontalOptions="FillAndExpand" />
                

                <Grid ColumnDefinitions="*, auto" Padding="10">

                    <StackLayout Grid.Column="0" Spacing="15">
                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Users }" HorizontalOptions="Start" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                        <Label Text="Chaves de Contatos" FontSize="{DynamicResource FontSizeMedium}" FontFamily="{DynamicResource FontBold}" TextColor="{DynamicResource textSecondary}" />
                    </StackLayout>

                    <StackLayout Grid.Column="1" Spacing="15" Padding="30,0,0,0">
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateToAddNewKeyPageContactCommand}" />
                        </StackLayout.GestureRecognizers>
                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ChevronRight }" HorizontalOptions="End" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                        <Label Text="Add" FontSize="{DynamicResource FontSizeMedium}" FontFamily="{DynamicResource FontBold}" TextColor="{DynamicResource textSecondary}" />
                    </StackLayout>
                </Grid>


                <CollectionView Grid.Row="0" ItemsSource="{Binding PixKeyListContact}" HeightRequest="270">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Horizontal" />
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>

                            <StackLayout Padding="2,0">
                                <Frame CornerRadius="10" Padding="15" BackgroundColor="{DynamicResource foreground_page}" WidthRequest="150">
                                    <StackLayout Spacing="15" HorizontalOptions="FillAndExpand">


                                        <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.UserCircle }" Grid.Column="0" Size="60" IconColor="{DynamicResource textSecondary}" VerticalOptions="Center" />

                                        <StackLayout Spacing="0">
                                            <Label Text="{Binding Viewer.NamePresentation}" TextColor="{DynamicResource textSecondary}" FontFamily="{DynamicResource FontBold}" FontSize="{StaticResource FontSizeMedium}"  />
                                            <Label Text="{Binding Viewer.KeyPresentation}" TextColor="{DynamicResource textSecondary}" MaxLines="1" LineBreakMode="MiddleTruncation" />
                                        </StackLayout>

                                        <BoxView HeightRequest="1" BackgroundColor="{DynamicResource foreground_page}" HorizontalOptions="FillAndExpand" />

                                        <Grid ColumnDefinitions="*,*">
                                            <!--<Frame Grid.Column="0" Style="{DynamicResource styleButtonActionListContact}">
                                                <Frame.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Command.CopyKeyCommand}" />
                                                </Frame.GestureRecognizers>-->
                                            <StackLayout Grid.Column="0">
                                                <StackLayout.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Command.CopyKeyCommand}" />
                                                </StackLayout.GestureRecognizers>

                                                <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Copy }" Style="{DynamicResource styleIconActionListContact}" />
                                                <Label Text="Copiar" Style="{DynamicResource styleTextOnButtonActionListContact}"/>
                                            </StackLayout>
                                            <!--</Frame>-->

                                            <!--<Frame Grid.Column="1" Style="{DynamicResource styleButtonActionListContact}">
                                                <Frame.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Command.EditKeyCommand}" />
                                                </Frame.GestureRecognizers>-->
                                            <StackLayout Grid.Column="1">
                                                <StackLayout.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Command.EditKeyCommand}" />
                                                </StackLayout.GestureRecognizers>

                                                <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Pen }" Style="{DynamicResource styleIconActionListContact}" />
                                                <Label Text="Editar" Style="{DynamicResource styleTextOnButtonActionListContact}"/>
                                            </StackLayout>
                                            <!--</Frame>-->

                                        </Grid>
                                    </StackLayout>
                                </Frame>
                            </StackLayout>

                        </DataTemplate>

                    </CollectionView.ItemTemplate>

                </CollectionView>

                <BoxView HeightRequest="1" BackgroundColor="{DynamicResource foreground_page}" HorizontalOptions="FillAndExpand" />
                <StackLayout Padding="10" Spacing="15">
                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.Rss }" HorizontalOptions="Start" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" />
                    <Label Text="Notícias" FontSize="{DynamicResource FontSizeMedium}" FontFamily="{DynamicResource FontBold}" TextColor="{DynamicResource textSecondary}" />
                </StackLayout>

                <controls:CustomCollectionView ItemsSource="{Binding CurrentFeedList}" SelectionMode="None" IsVisible="{Binding CurrentFeedList, Converter={StaticResource HasDataConverter}}" HeightRequest="1100">

                    <controls:CustomCollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout Padding="5,2">

                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Command.NavigateToWebViewCommand }"/>
                                </StackLayout.GestureRecognizers>

                                <Frame BackgroundColor="{StaticResource foreground_page}" CornerRadius="15" Padding="0">

                                    <StackLayout>
                                        <!--<controls:CustomImage Source="{Binding Image}" Aspect="Fill" HeightRequest="200" />-->
                                        <StackLayout Padding="15">
                                            <Label Text="{Binding Source}" FontSize="{DynamicResource FontSizeSmall}" TextColor="{DynamicResource textPrimary}" FontFamily="{DynamicResource FontSemiBold}" />

                                            <Grid ColumnDefinitions="auto,*" ColumnSpacing="15">
                                                <StackLayout Grid.Column="0">
                                                    <controls:CustomImage Source="{Binding Image}" Aspect="Fill" HeightRequest="80" WidthRequest="80" />
                                                </StackLayout>

                                                <Label Grid.Column="1" Text="{Binding Title}" MaxLines="4" LineBreakMode="TailTruncation" FontSize="{DynamicResource FontSizeSmall}" TextColor="{DynamicResource textSecondary}" VerticalOptions="FillAndExpand"/>
                                            </Grid>

                                            <StackLayout Orientation="Horizontal">
                                                <Label Text="{Binding Viewer.PublishDateDisplay}" FontSize="{DynamicResource FontSizeMicro}" TextColor="{DynamicResource textSecondary}" HorizontalOptions="StartAndExpand" VerticalOptions="EndAndExpand" />
                                                <StackLayout Padding="15,15,0,0">
                                                    <StackLayout.GestureRecognizers>
                                                        <TapGestureRecognizer Command="{Binding Command.ShareCommand }"/>
                                                    </StackLayout.GestureRecognizers>
                                                    <controls:CustomIcon Glyph="{x:Static icons:FontAwesomeSolid.ShareAlt }" Size="{DynamicResource FontSizeMedium}"  IconColor="{DynamicResource textSecondary}" HorizontalOptions="CenterAndExpand" />
                                                </StackLayout>
                                            </StackLayout>
                                        </StackLayout>
                                    </StackLayout>

                                </Frame>

                            </StackLayout>
                        </DataTemplate>
                    </controls:CustomCollectionView.ItemTemplate>
                </controls:CustomCollectionView>

            </StackLayout>
        </ScrollView>


    </ContentPage.Content>
</ContentPage>